import React from 'react';

const Settings = ({nickname}) => {
                 return(
                    <div>
                        Welcome to the Settings page, {nickname}
                    </div>
                 );
             }

export default Settings;